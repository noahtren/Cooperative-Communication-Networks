"""Brute force graph matching with TensorFlow code
"""

import code


def loss(adj, nf, adj_pred, nf_pred):
  pass


def minimum_loss_permutation(adj, nf, adj_pred, nf_pred):
  code.interact(local={**locals(), **globals()})